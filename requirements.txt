# Python packages needed for local paper-artifact checks and rendering.
# This does not install external simulator, RTL, or toolchain dependencies such
# as gem5, Vivado, MSYS/UCRT, AArch64 cross tools, or public benchmark sources.
matplotlib>=3.8
pypdf>=5
python-docx>=1.1
reportlab>=4
Pillow>=10
PyMuPDF>=1.24
