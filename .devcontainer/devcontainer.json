{
  "name": "COPPER artifact",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "containerEnv": {
    "COPPER_ENVIRONMENT": "codespaces"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "mshr-h.veriloghdl"
      ]
    }
  },
  "postCreateCommand": "python3 -m pip install --break-system-packages -r requirements.txt pytest numpy pandas && make check-toolchain"
}
